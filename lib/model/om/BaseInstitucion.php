<?php


/**
 * Base class that represents a row from the 'institucion' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.5.6 on:
 *
 * Thu Feb 20 13:36:21 2014
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseInstitucion extends BaseObject  implements Persistent
{

	/**
	 * Peer class name
	 */
	const PEER = 'InstitucionPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        InstitucionPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        string
	 */
	protected $id;

	/**
	 * The value for the nombre field.
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the correo field.
	 * @var        string
	 */
	protected $correo;

	/**
	 * The value for the rif field.
	 * @var        string
	 */
	protected $rif;

	/**
	 * The value for the ubicacion field.
	 * @var        string
	 */
	protected $ubicacion;

	/**
	 * The value for the id_tipoinstitucion field.
	 * @var        int
	 */
	protected $id_tipoinstitucion;

	/**
	 * The value for the sector field.
	 * @var        string
	 */
	protected $sector;

	/**
	 * The value for the responsable field.
	 * @var        string
	 */
	protected $responsable;

	/**
	 * The value for the publica field.
	 * @var        boolean
	 */
	protected $publica;

	/**
	 * The value for the id_pais field.
	 * @var        int
	 */
	protected $id_pais;

	/**
	 * The value for the id_municipio field.
	 * @var        int
	 */
	protected $id_municipio;

	/**
	 * The value for the id_parroquia field.
	 * @var        int
	 */
	protected $id_parroquia;

	/**
	 * @var        Tipoinstitucion
	 */
	protected $aTipoinstitucion;

	/**
	 * @var        Pais
	 */
	protected $aPais;

	/**
	 * @var        Municipio
	 */
	protected $aMunicipio;

	/**
	 * @var        Parroquia
	 */
	protected $aParroquia;

	/**
	 * @var        array Historico[] Collection to store aggregation of Historico objects.
	 */
	protected $collHistoricos;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * 
	 * @return     string
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [nombre] column value.
	 * 
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [correo] column value.
	 * 
	 * @return     string
	 */
	public function getCorreo()
	{
		return $this->correo;
	}

	/**
	 * Get the [rif] column value.
	 * 
	 * @return     string
	 */
	public function getRif()
	{
		return $this->rif;
	}

	/**
	 * Get the [ubicacion] column value.
	 * 
	 * @return     string
	 */
	public function getUbicacion()
	{
		return $this->ubicacion;
	}

	/**
	 * Get the [id_tipoinstitucion] column value.
	 * 
	 * @return     int
	 */
	public function getIdTipoinstitucion()
	{
		return $this->id_tipoinstitucion;
	}

	/**
	 * Get the [sector] column value.
	 * 
	 * @return     string
	 */
	public function getSector()
	{
		return $this->sector;
	}

	/**
	 * Get the [responsable] column value.
	 * 
	 * @return     string
	 */
	public function getResponsable()
	{
		return $this->responsable;
	}

	/**
	 * Get the [publica] column value.
	 * 
	 * @return     boolean
	 */
	public function getPublica()
	{
		return $this->publica;
	}

	/**
	 * Get the [id_pais] column value.
	 * 
	 * @return     int
	 */
	public function getIdPais()
	{
		return $this->id_pais;
	}

	/**
	 * Get the [id_municipio] column value.
	 * 
	 * @return     int
	 */
	public function getIdMunicipio()
	{
		return $this->id_municipio;
	}

	/**
	 * Get the [id_parroquia] column value.
	 * 
	 * @return     int
	 */
	public function getIdParroquia()
	{
		return $this->id_parroquia;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = InstitucionPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v) {
			$this->nombre = $v;
			$this->modifiedColumns[] = InstitucionPeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [correo] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setCorreo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->correo !== $v) {
			$this->correo = $v;
			$this->modifiedColumns[] = InstitucionPeer::CORREO;
		}

		return $this;
	} // setCorreo()

	/**
	 * Set the value of [rif] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setRif($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->rif !== $v) {
			$this->rif = $v;
			$this->modifiedColumns[] = InstitucionPeer::RIF;
		}

		return $this;
	} // setRif()

	/**
	 * Set the value of [ubicacion] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setUbicacion($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ubicacion !== $v) {
			$this->ubicacion = $v;
			$this->modifiedColumns[] = InstitucionPeer::UBICACION;
		}

		return $this;
	} // setUbicacion()

	/**
	 * Set the value of [id_tipoinstitucion] column.
	 * 
	 * @param      int $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setIdTipoinstitucion($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_tipoinstitucion !== $v) {
			$this->id_tipoinstitucion = $v;
			$this->modifiedColumns[] = InstitucionPeer::ID_TIPOINSTITUCION;
		}

		if ($this->aTipoinstitucion !== null && $this->aTipoinstitucion->getId() !== $v) {
			$this->aTipoinstitucion = null;
		}

		return $this;
	} // setIdTipoinstitucion()

	/**
	 * Set the value of [sector] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setSector($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->sector !== $v) {
			$this->sector = $v;
			$this->modifiedColumns[] = InstitucionPeer::SECTOR;
		}

		return $this;
	} // setSector()

	/**
	 * Set the value of [responsable] column.
	 * 
	 * @param      string $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setResponsable($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->responsable !== $v) {
			$this->responsable = $v;
			$this->modifiedColumns[] = InstitucionPeer::RESPONSABLE;
		}

		return $this;
	} // setResponsable()

	/**
	 * Set the value of [publica] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setPublica($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->publica !== $v) {
			$this->publica = $v;
			$this->modifiedColumns[] = InstitucionPeer::PUBLICA;
		}

		return $this;
	} // setPublica()

	/**
	 * Set the value of [id_pais] column.
	 * 
	 * @param      int $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setIdPais($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_pais !== $v) {
			$this->id_pais = $v;
			$this->modifiedColumns[] = InstitucionPeer::ID_PAIS;
		}

		if ($this->aPais !== null && $this->aPais->getId() !== $v) {
			$this->aPais = null;
		}

		return $this;
	} // setIdPais()

	/**
	 * Set the value of [id_municipio] column.
	 * 
	 * @param      int $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setIdMunicipio($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_municipio !== $v) {
			$this->id_municipio = $v;
			$this->modifiedColumns[] = InstitucionPeer::ID_MUNICIPIO;
		}

		if ($this->aMunicipio !== null && $this->aMunicipio->getId() !== $v) {
			$this->aMunicipio = null;
		}

		return $this;
	} // setIdMunicipio()

	/**
	 * Set the value of [id_parroquia] column.
	 * 
	 * @param      int $v new value
	 * @return     Institucion The current object (for fluent API support)
	 */
	public function setIdParroquia($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id_parroquia !== $v) {
			$this->id_parroquia = $v;
			$this->modifiedColumns[] = InstitucionPeer::ID_PARROQUIA;
		}

		if ($this->aParroquia !== null && $this->aParroquia->getId() !== $v) {
			$this->aParroquia = null;
		}

		return $this;
	} // setIdParroquia()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->nombre = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->correo = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->rif = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->ubicacion = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->id_tipoinstitucion = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->sector = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->responsable = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->publica = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
			$this->id_pais = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->id_municipio = ($row[$startcol + 10] !== null) ? (int) $row[$startcol + 10] : null;
			$this->id_parroquia = ($row[$startcol + 11] !== null) ? (int) $row[$startcol + 11] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			return $startcol + 12; // 12 = InstitucionPeer::NUM_COLUMNS - InstitucionPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Institucion object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aTipoinstitucion !== null && $this->id_tipoinstitucion !== $this->aTipoinstitucion->getId()) {
			$this->aTipoinstitucion = null;
		}
		if ($this->aPais !== null && $this->id_pais !== $this->aPais->getId()) {
			$this->aPais = null;
		}
		if ($this->aMunicipio !== null && $this->id_municipio !== $this->aMunicipio->getId()) {
			$this->aMunicipio = null;
		}
		if ($this->aParroquia !== null && $this->id_parroquia !== $this->aParroquia->getId()) {
			$this->aParroquia = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(InstitucionPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = InstitucionPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aTipoinstitucion = null;
			$this->aPais = null;
			$this->aMunicipio = null;
			$this->aParroquia = null;
			$this->collHistoricos = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(InstitucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseInstitucion:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			    return;
			  }
			}

			if ($ret) {
				InstitucionQuery::create()
					->filterByPrimaryKey($this->getPrimaryKey())
					->delete($con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseInstitucion:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$con->commit();
				$this->setDeleted(true);
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(InstitucionPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseInstitucion:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			  	$con->commit();
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseInstitucion:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				InstitucionPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTipoinstitucion !== null) {
				if ($this->aTipoinstitucion->isModified() || $this->aTipoinstitucion->isNew()) {
					$affectedRows += $this->aTipoinstitucion->save($con);
				}
				$this->setTipoinstitucion($this->aTipoinstitucion);
			}

			if ($this->aPais !== null) {
				if ($this->aPais->isModified() || $this->aPais->isNew()) {
					$affectedRows += $this->aPais->save($con);
				}
				$this->setPais($this->aPais);
			}

			if ($this->aMunicipio !== null) {
				if ($this->aMunicipio->isModified() || $this->aMunicipio->isNew()) {
					$affectedRows += $this->aMunicipio->save($con);
				}
				$this->setMunicipio($this->aMunicipio);
			}

			if ($this->aParroquia !== null) {
				if ($this->aParroquia->isModified() || $this->aParroquia->isNew()) {
					$affectedRows += $this->aParroquia->save($con);
				}
				$this->setParroquia($this->aParroquia);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = InstitucionPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$criteria = $this->buildCriteria();
					if ($criteria->keyContainsValue(InstitucionPeer::ID) ) {
						throw new PropelException('Cannot insert a value for auto-increment primary key ('.InstitucionPeer::ID.')');
					}

					$pk = BasePeer::doInsert($criteria, $con);
					$affectedRows += 1;
					$this->setId($pk);  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					$affectedRows += InstitucionPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collHistoricos !== null) {
				foreach ($this->collHistoricos as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aTipoinstitucion !== null) {
				if (!$this->aTipoinstitucion->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aTipoinstitucion->getValidationFailures());
				}
			}

			if ($this->aPais !== null) {
				if (!$this->aPais->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aPais->getValidationFailures());
				}
			}

			if ($this->aMunicipio !== null) {
				if (!$this->aMunicipio->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aMunicipio->getValidationFailures());
				}
			}

			if ($this->aParroquia !== null) {
				if (!$this->aParroquia->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aParroquia->getValidationFailures());
				}
			}


			if (($retval = InstitucionPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collHistoricos !== null) {
					foreach ($this->collHistoricos as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = InstitucionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getNombre();
				break;
			case 2:
				return $this->getCorreo();
				break;
			case 3:
				return $this->getRif();
				break;
			case 4:
				return $this->getUbicacion();
				break;
			case 5:
				return $this->getIdTipoinstitucion();
				break;
			case 6:
				return $this->getSector();
				break;
			case 7:
				return $this->getResponsable();
				break;
			case 8:
				return $this->getPublica();
				break;
			case 9:
				return $this->getIdPais();
				break;
			case 10:
				return $this->getIdMunicipio();
				break;
			case 11:
				return $this->getIdParroquia();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 *                    Defaults to BasePeer::TYPE_PHPNAME.
	 * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to TRUE.
	 * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
	 *
	 * @return    array an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $includeForeignObjects = false)
	{
		$keys = InstitucionPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getNombre(),
			$keys[2] => $this->getCorreo(),
			$keys[3] => $this->getRif(),
			$keys[4] => $this->getUbicacion(),
			$keys[5] => $this->getIdTipoinstitucion(),
			$keys[6] => $this->getSector(),
			$keys[7] => $this->getResponsable(),
			$keys[8] => $this->getPublica(),
			$keys[9] => $this->getIdPais(),
			$keys[10] => $this->getIdMunicipio(),
			$keys[11] => $this->getIdParroquia(),
		);
		if ($includeForeignObjects) {
			if (null !== $this->aTipoinstitucion) {
				$result['Tipoinstitucion'] = $this->aTipoinstitucion->toArray($keyType, $includeLazyLoadColumns, true);
			}
			if (null !== $this->aPais) {
				$result['Pais'] = $this->aPais->toArray($keyType, $includeLazyLoadColumns, true);
			}
			if (null !== $this->aMunicipio) {
				$result['Municipio'] = $this->aMunicipio->toArray($keyType, $includeLazyLoadColumns, true);
			}
			if (null !== $this->aParroquia) {
				$result['Parroquia'] = $this->aParroquia->toArray($keyType, $includeLazyLoadColumns, true);
			}
		}
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = InstitucionPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setNombre($value);
				break;
			case 2:
				$this->setCorreo($value);
				break;
			case 3:
				$this->setRif($value);
				break;
			case 4:
				$this->setUbicacion($value);
				break;
			case 5:
				$this->setIdTipoinstitucion($value);
				break;
			case 6:
				$this->setSector($value);
				break;
			case 7:
				$this->setResponsable($value);
				break;
			case 8:
				$this->setPublica($value);
				break;
			case 9:
				$this->setIdPais($value);
				break;
			case 10:
				$this->setIdMunicipio($value);
				break;
			case 11:
				$this->setIdParroquia($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = InstitucionPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNombre($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setCorreo($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setRif($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setUbicacion($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setIdTipoinstitucion($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setSector($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setResponsable($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setPublica($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setIdPais($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setIdMunicipio($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setIdParroquia($arr[$keys[11]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(InstitucionPeer::DATABASE_NAME);

		if ($this->isColumnModified(InstitucionPeer::ID)) $criteria->add(InstitucionPeer::ID, $this->id);
		if ($this->isColumnModified(InstitucionPeer::NOMBRE)) $criteria->add(InstitucionPeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(InstitucionPeer::CORREO)) $criteria->add(InstitucionPeer::CORREO, $this->correo);
		if ($this->isColumnModified(InstitucionPeer::RIF)) $criteria->add(InstitucionPeer::RIF, $this->rif);
		if ($this->isColumnModified(InstitucionPeer::UBICACION)) $criteria->add(InstitucionPeer::UBICACION, $this->ubicacion);
		if ($this->isColumnModified(InstitucionPeer::ID_TIPOINSTITUCION)) $criteria->add(InstitucionPeer::ID_TIPOINSTITUCION, $this->id_tipoinstitucion);
		if ($this->isColumnModified(InstitucionPeer::SECTOR)) $criteria->add(InstitucionPeer::SECTOR, $this->sector);
		if ($this->isColumnModified(InstitucionPeer::RESPONSABLE)) $criteria->add(InstitucionPeer::RESPONSABLE, $this->responsable);
		if ($this->isColumnModified(InstitucionPeer::PUBLICA)) $criteria->add(InstitucionPeer::PUBLICA, $this->publica);
		if ($this->isColumnModified(InstitucionPeer::ID_PAIS)) $criteria->add(InstitucionPeer::ID_PAIS, $this->id_pais);
		if ($this->isColumnModified(InstitucionPeer::ID_MUNICIPIO)) $criteria->add(InstitucionPeer::ID_MUNICIPIO, $this->id_municipio);
		if ($this->isColumnModified(InstitucionPeer::ID_PARROQUIA)) $criteria->add(InstitucionPeer::ID_PARROQUIA, $this->id_parroquia);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(InstitucionPeer::DATABASE_NAME);
		$criteria->add(InstitucionPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Returns true if the primary key for this object is null.
	 * @return     boolean
	 */
	public function isPrimaryKeyNull()
	{
		return null === $this->getId();
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Institucion (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{
		$copyObj->setNombre($this->nombre);
		$copyObj->setCorreo($this->correo);
		$copyObj->setRif($this->rif);
		$copyObj->setUbicacion($this->ubicacion);
		$copyObj->setIdTipoinstitucion($this->id_tipoinstitucion);
		$copyObj->setSector($this->sector);
		$copyObj->setResponsable($this->responsable);
		$copyObj->setPublica($this->publica);
		$copyObj->setIdPais($this->id_pais);
		$copyObj->setIdMunicipio($this->id_municipio);
		$copyObj->setIdParroquia($this->id_parroquia);

		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getHistoricos() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addHistorico($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);
		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value
	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Institucion Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     InstitucionPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new InstitucionPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Tipoinstitucion object.
	 *
	 * @param      Tipoinstitucion $v
	 * @return     Institucion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setTipoinstitucion(Tipoinstitucion $v = null)
	{
		if ($v === null) {
			$this->setIdTipoinstitucion(NULL);
		} else {
			$this->setIdTipoinstitucion($v->getId());
		}

		$this->aTipoinstitucion = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Tipoinstitucion object, it will not be re-added.
		if ($v !== null) {
			$v->addInstitucion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Tipoinstitucion object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Tipoinstitucion The associated Tipoinstitucion object.
	 * @throws     PropelException
	 */
	public function getTipoinstitucion(PropelPDO $con = null)
	{
		if ($this->aTipoinstitucion === null && ($this->id_tipoinstitucion !== null)) {
			$this->aTipoinstitucion = TipoinstitucionQuery::create()->findPk($this->id_tipoinstitucion, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aTipoinstitucion->addInstitucions($this);
			 */
		}
		return $this->aTipoinstitucion;
	}

	/**
	 * Declares an association between this object and a Pais object.
	 *
	 * @param      Pais $v
	 * @return     Institucion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setPais(Pais $v = null)
	{
		if ($v === null) {
			$this->setIdPais(NULL);
		} else {
			$this->setIdPais($v->getId());
		}

		$this->aPais = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Pais object, it will not be re-added.
		if ($v !== null) {
			$v->addInstitucion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Pais object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Pais The associated Pais object.
	 * @throws     PropelException
	 */
	public function getPais(PropelPDO $con = null)
	{
		if ($this->aPais === null && ($this->id_pais !== null)) {
			$this->aPais = PaisQuery::create()->findPk($this->id_pais, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aPais->addInstitucions($this);
			 */
		}
		return $this->aPais;
	}

	/**
	 * Declares an association between this object and a Municipio object.
	 *
	 * @param      Municipio $v
	 * @return     Institucion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setMunicipio(Municipio $v = null)
	{
		if ($v === null) {
			$this->setIdMunicipio(NULL);
		} else {
			$this->setIdMunicipio($v->getId());
		}

		$this->aMunicipio = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Municipio object, it will not be re-added.
		if ($v !== null) {
			$v->addInstitucion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Municipio object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Municipio The associated Municipio object.
	 * @throws     PropelException
	 */
	public function getMunicipio(PropelPDO $con = null)
	{
		if ($this->aMunicipio === null && ($this->id_municipio !== null)) {
			$this->aMunicipio = MunicipioQuery::create()->findPk($this->id_municipio, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aMunicipio->addInstitucions($this);
			 */
		}
		return $this->aMunicipio;
	}

	/**
	 * Declares an association between this object and a Parroquia object.
	 *
	 * @param      Parroquia $v
	 * @return     Institucion The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setParroquia(Parroquia $v = null)
	{
		if ($v === null) {
			$this->setIdParroquia(NULL);
		} else {
			$this->setIdParroquia($v->getId());
		}

		$this->aParroquia = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Parroquia object, it will not be re-added.
		if ($v !== null) {
			$v->addInstitucion($this);
		}

		return $this;
	}


	/**
	 * Get the associated Parroquia object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Parroquia The associated Parroquia object.
	 * @throws     PropelException
	 */
	public function getParroquia(PropelPDO $con = null)
	{
		if ($this->aParroquia === null && ($this->id_parroquia !== null)) {
			$this->aParroquia = ParroquiaQuery::create()->findPk($this->id_parroquia, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aParroquia->addInstitucions($this);
			 */
		}
		return $this->aParroquia;
	}

	/**
	 * Clears out the collHistoricos collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addHistoricos()
	 */
	public function clearHistoricos()
	{
		$this->collHistoricos = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collHistoricos collection.
	 *
	 * By default this just sets the collHistoricos collection to an empty array (like clearcollHistoricos());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initHistoricos()
	{
		$this->collHistoricos = new PropelObjectCollection();
		$this->collHistoricos->setModel('Historico');
	}

	/**
	 * Gets an array of Historico objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Institucion is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 * @throws     PropelException
	 */
	public function getHistoricos($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collHistoricos || null !== $criteria) {
			if ($this->isNew() && null === $this->collHistoricos) {
				// return empty collection
				$this->initHistoricos();
			} else {
				$collHistoricos = HistoricoQuery::create(null, $criteria)
					->filterByInstitucion($this)
					->find($con);
				if (null !== $criteria) {
					return $collHistoricos;
				}
				$this->collHistoricos = $collHistoricos;
			}
		}
		return $this->collHistoricos;
	}

	/**
	 * Returns the number of related Historico objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Historico objects.
	 * @throws     PropelException
	 */
	public function countHistoricos(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collHistoricos || null !== $criteria) {
			if ($this->isNew() && null === $this->collHistoricos) {
				return 0;
			} else {
				$query = HistoricoQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByInstitucion($this)
					->count($con);
			}
		} else {
			return count($this->collHistoricos);
		}
	}

	/**
	 * Method called to associate a Historico object to this object
	 * through the Historico foreign key attribute.
	 *
	 * @param      Historico $l Historico
	 * @return     void
	 * @throws     PropelException
	 */
	public function addHistorico(Historico $l)
	{
		if ($this->collHistoricos === null) {
			$this->initHistoricos();
		}
		if (!$this->collHistoricos->contains($l)) { // only add it if the **same** object is not already associated
			$this->collHistoricos[]= $l;
			$l->setInstitucion($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Institucion is new, it will return
	 * an empty collection; or if this Institucion has previously
	 * been saved, it will retrieve related Historicos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Institucion.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 */
	public function getHistoricosJoinEdicion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = HistoricoQuery::create(null, $criteria);
		$query->joinWith('Edicion', $join_behavior);

		return $this->getHistoricos($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Institucion is new, it will return
	 * an empty collection; or if this Institucion has previously
	 * been saved, it will retrieve related Historicos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Institucion.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 */
	public function getHistoricosJoinStatus($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = HistoricoQuery::create(null, $criteria);
		$query->joinWith('Status', $join_behavior);

		return $this->getHistoricos($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Institucion is new, it will return
	 * an empty collection; or if this Institucion has previously
	 * been saved, it will retrieve related Historicos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Institucion.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 */
	public function getHistoricosJoinImpresion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = HistoricoQuery::create(null, $criteria);
		$query->joinWith('Impresion', $join_behavior);

		return $this->getHistoricos($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Institucion is new, it will return
	 * an empty collection; or if this Institucion has previously
	 * been saved, it will retrieve related Historicos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Institucion.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 */
	public function getHistoricosJoinDistribucion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = HistoricoQuery::create(null, $criteria);
		$query->joinWith('Distribucion', $join_behavior);

		return $this->getHistoricos($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Institucion is new, it will return
	 * an empty collection; or if this Institucion has previously
	 * been saved, it will retrieve related Historicos from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Institucion.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Historico[] List of Historico objects
	 */
	public function getHistoricosJoinVenta($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = HistoricoQuery::create(null, $criteria);
		$query->joinWith('Venta', $join_behavior);

		return $this->getHistoricos($query, $con);
	}

	/**
	 * Clears the current object and sets all attributes to their default values
	 */
	public function clear()
	{
		$this->id = null;
		$this->nombre = null;
		$this->correo = null;
		$this->rif = null;
		$this->ubicacion = null;
		$this->id_tipoinstitucion = null;
		$this->sector = null;
		$this->responsable = null;
		$this->publica = null;
		$this->id_pais = null;
		$this->id_municipio = null;
		$this->id_parroquia = null;
		$this->alreadyInSave = false;
		$this->alreadyInValidation = false;
		$this->clearAllReferences();
		$this->resetModified();
		$this->setNew(true);
		$this->setDeleted(false);
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collHistoricos) {
				foreach ((array) $this->collHistoricos as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collHistoricos = null;
		$this->aTipoinstitucion = null;
		$this->aPais = null;
		$this->aMunicipio = null;
		$this->aParroquia = null;
	}

	/**
	 * Catches calls to virtual methods
	 */
	public function __call($name, $params)
	{
		// symfony_behaviors behavior
		if ($callable = sfMixer::getCallable('BaseInstitucion:' . $name))
		{
		  array_unshift($params, $this);
		  return call_user_func_array($callable, $params);
		}

		if (preg_match('/get(\w+)/', $name, $matches)) {
			$virtualColumn = $matches[1];
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
			// no lcfirst in php<5.3...
			$virtualColumn[0] = strtolower($virtualColumn[0]);
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
		}
		return parent::__call($name, $params);
	}

} // BaseInstitucion
