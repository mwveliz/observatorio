<?php


/**
 * Base class that represents a row from the 'autor' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.5.6 on:
 *
 * Thu Apr 10 10:28:43 2014
 *
 * @package    propel.generator.lib.model.om
 */
abstract class BaseAutor extends BaseObject  implements Persistent
{

	/**
	 * Peer class name
	 */
	const PEER = 'AutorPeer';

	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        AutorPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        string
	 */
	protected $id;

	/**
	 * The value for the ficha_autor field.
	 * @var        string
	 */
	protected $ficha_autor;

	/**
	 * The value for the inedito field.
	 * @var        boolean
	 */
	protected $inedito;

	/**
	 * The value for the sexo field.
	 * @var        boolean
	 */
	protected $sexo;

	/**
	 * The value for the nacionalidad field.
	 * @var        string
	 */
	protected $nacionalidad;

	/**
	 * The value for the fecha_nacimiento field.
	 * @var        int
	 */
	protected $fecha_nacimiento;

	/**
	 * The value for the nombre field.
	 * @var        string
	 */
	protected $nombre;

	/**
	 * The value for the apellido field.
	 * @var        string
	 */
	protected $apellido;

	/**
	 * The value for the vive field.
	 * @var        boolean
	 */
	protected $vive;

	/**
	 * The value for the tipoautor field.
	 * @var        int
	 */
	protected $tipoautor;

	/**
	 * @var        Autor
	 */
	protected $aAutorRelatedByTipoautor;

	/**
	 * @var        array Autor[] Collection to store aggregation of Autor objects.
	 */
	protected $collAutorsRelatedById;

	/**
	 * @var        array Libro[] Collection to store aggregation of Libro objects.
	 */
	protected $collLibros;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	/**
	 * Get the [id] column value.
	 * 
	 * @return     string
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [ficha_autor] column value.
	 * 
	 * @return     string
	 */
	public function getFichaAutor()
	{
		return $this->ficha_autor;
	}

	/**
	 * Get the [inedito] column value.
	 * 
	 * @return     boolean
	 */
	public function getInedito()
	{
		return $this->inedito;
	}

	/**
	 * Get the [sexo] column value.
	 * 
	 * @return     boolean
	 */
	public function getSexo()
	{
		return $this->sexo;
	}

	/**
	 * Get the [nacionalidad] column value.
	 * 
	 * @return     string
	 */
	public function getNacionalidad()
	{
		return $this->nacionalidad;
	}

	/**
	 * Get the [fecha_nacimiento] column value.
	 * 
	 * @return     int
	 */
	public function getFechaNacimiento()
	{
		return $this->fecha_nacimiento;
	}

	/**
	 * Get the [nombre] column value.
	 * 
	 * @return     string
	 */
	public function getNombre()
	{
		return $this->nombre;
	}

	/**
	 * Get the [apellido] column value.
	 * 
	 * @return     string
	 */
	public function getApellido()
	{
		return $this->apellido;
	}

	/**
	 * Get the [vive] column value.
	 * 
	 * @return     boolean
	 */
	public function getVive()
	{
		return $this->vive;
	}

	/**
	 * Get the [tipoautor] column value.
	 * 
	 * @return     int
	 */
	public function getTipoautor()
	{
		return $this->tipoautor;
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      string $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = AutorPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [ficha_autor] column.
	 * 
	 * @param      string $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setFichaAutor($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->ficha_autor !== $v) {
			$this->ficha_autor = $v;
			$this->modifiedColumns[] = AutorPeer::FICHA_AUTOR;
		}

		return $this;
	} // setFichaAutor()

	/**
	 * Set the value of [inedito] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setInedito($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->inedito !== $v) {
			$this->inedito = $v;
			$this->modifiedColumns[] = AutorPeer::INEDITO;
		}

		return $this;
	} // setInedito()

	/**
	 * Set the value of [sexo] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setSexo($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->sexo !== $v) {
			$this->sexo = $v;
			$this->modifiedColumns[] = AutorPeer::SEXO;
		}

		return $this;
	} // setSexo()

	/**
	 * Set the value of [nacionalidad] column.
	 * 
	 * @param      string $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setNacionalidad($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nacionalidad !== $v) {
			$this->nacionalidad = $v;
			$this->modifiedColumns[] = AutorPeer::NACIONALIDAD;
		}

		return $this;
	} // setNacionalidad()

	/**
	 * Set the value of [fecha_nacimiento] column.
	 * 
	 * @param      int $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setFechaNacimiento($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->fecha_nacimiento !== $v) {
			$this->fecha_nacimiento = $v;
			$this->modifiedColumns[] = AutorPeer::FECHA_NACIMIENTO;
		}

		return $this;
	} // setFechaNacimiento()

	/**
	 * Set the value of [nombre] column.
	 * 
	 * @param      string $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setNombre($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nombre !== $v) {
			$this->nombre = $v;
			$this->modifiedColumns[] = AutorPeer::NOMBRE;
		}

		return $this;
	} // setNombre()

	/**
	 * Set the value of [apellido] column.
	 * 
	 * @param      string $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setApellido($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->apellido !== $v) {
			$this->apellido = $v;
			$this->modifiedColumns[] = AutorPeer::APELLIDO;
		}

		return $this;
	} // setApellido()

	/**
	 * Set the value of [vive] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setVive($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->vive !== $v) {
			$this->vive = $v;
			$this->modifiedColumns[] = AutorPeer::VIVE;
		}

		return $this;
	} // setVive()

	/**
	 * Set the value of [tipoautor] column.
	 * 
	 * @param      int $v new value
	 * @return     Autor The current object (for fluent API support)
	 */
	public function setTipoautor($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->tipoautor !== $v) {
			$this->tipoautor = $v;
			$this->modifiedColumns[] = AutorPeer::TIPOAUTOR;
		}

		if ($this->aAutorRelatedByTipoautor !== null && $this->aAutorRelatedByTipoautor->getId() !== $v) {
			$this->aAutorRelatedByTipoautor = null;
		}

		return $this;
	} // setTipoautor()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->ficha_autor = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->inedito = ($row[$startcol + 2] !== null) ? (boolean) $row[$startcol + 2] : null;
			$this->sexo = ($row[$startcol + 3] !== null) ? (boolean) $row[$startcol + 3] : null;
			$this->nacionalidad = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->fecha_nacimiento = ($row[$startcol + 5] !== null) ? (int) $row[$startcol + 5] : null;
			$this->nombre = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->apellido = ($row[$startcol + 7] !== null) ? (string) $row[$startcol + 7] : null;
			$this->vive = ($row[$startcol + 8] !== null) ? (boolean) $row[$startcol + 8] : null;
			$this->tipoautor = ($row[$startcol + 9] !== null) ? (int) $row[$startcol + 9] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			return $startcol + 10; // 10 = AutorPeer::NUM_COLUMNS - AutorPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Autor object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aAutorRelatedByTipoautor !== null && $this->tipoautor !== $this->aAutorRelatedByTipoautor->getId()) {
			$this->aAutorRelatedByTipoautor = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AutorPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = AutorPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aAutorRelatedByTipoautor = null;
			$this->collAutorsRelatedById = null;

			$this->collLibros = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AutorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAutor:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			    return;
			  }
			}

			if ($ret) {
				AutorQuery::create()
					->filterByPrimaryKey($this->getPrimaryKey())
					->delete($con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAutor:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$con->commit();
				$this->setDeleted(true);
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(AutorPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}

		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseAutor:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			  	$con->commit();
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseAutor:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				AutorPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAutorRelatedByTipoautor !== null) {
				if ($this->aAutorRelatedByTipoautor->isModified() || $this->aAutorRelatedByTipoautor->isNew()) {
					$affectedRows += $this->aAutorRelatedByTipoautor->save($con);
				}
				$this->setAutorRelatedByTipoautor($this->aAutorRelatedByTipoautor);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = AutorPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$criteria = $this->buildCriteria();
					if ($criteria->keyContainsValue(AutorPeer::ID) ) {
						throw new PropelException('Cannot insert a value for auto-increment primary key ('.AutorPeer::ID.')');
					}

					$pk = BasePeer::doInsert($criteria, $con);
					$affectedRows += 1;
					$this->setId($pk);  //[IMV] update autoincrement primary key
					$this->setNew(false);
				} else {
					$affectedRows += AutorPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAutorsRelatedById !== null) {
				foreach ($this->collAutorsRelatedById as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collLibros !== null) {
				foreach ($this->collLibros as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aAutorRelatedByTipoautor !== null) {
				if (!$this->aAutorRelatedByTipoautor->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aAutorRelatedByTipoautor->getValidationFailures());
				}
			}


			if (($retval = AutorPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAutorsRelatedById !== null) {
					foreach ($this->collAutorsRelatedById as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collLibros !== null) {
					foreach ($this->collLibros as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AutorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getFichaAutor();
				break;
			case 2:
				return $this->getInedito();
				break;
			case 3:
				return $this->getSexo();
				break;
			case 4:
				return $this->getNacionalidad();
				break;
			case 5:
				return $this->getFechaNacimiento();
				break;
			case 6:
				return $this->getNombre();
				break;
			case 7:
				return $this->getApellido();
				break;
			case 8:
				return $this->getVive();
				break;
			case 9:
				return $this->getTipoautor();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param     string  $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 *                    BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 *                    Defaults to BasePeer::TYPE_PHPNAME.
	 * @param     boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns. Defaults to TRUE.
	 * @param     boolean $includeForeignObjects (optional) Whether to include hydrated related objects. Default to FALSE.
	 *
	 * @return    array an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true, $includeForeignObjects = false)
	{
		$keys = AutorPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getFichaAutor(),
			$keys[2] => $this->getInedito(),
			$keys[3] => $this->getSexo(),
			$keys[4] => $this->getNacionalidad(),
			$keys[5] => $this->getFechaNacimiento(),
			$keys[6] => $this->getNombre(),
			$keys[7] => $this->getApellido(),
			$keys[8] => $this->getVive(),
			$keys[9] => $this->getTipoautor(),
		);
		if ($includeForeignObjects) {
			if (null !== $this->aAutorRelatedByTipoautor) {
				$result['AutorRelatedByTipoautor'] = $this->aAutorRelatedByTipoautor->toArray($keyType, $includeLazyLoadColumns, true);
			}
		}
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = AutorPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setFichaAutor($value);
				break;
			case 2:
				$this->setInedito($value);
				break;
			case 3:
				$this->setSexo($value);
				break;
			case 4:
				$this->setNacionalidad($value);
				break;
			case 5:
				$this->setFechaNacimiento($value);
				break;
			case 6:
				$this->setNombre($value);
				break;
			case 7:
				$this->setApellido($value);
				break;
			case 8:
				$this->setVive($value);
				break;
			case 9:
				$this->setTipoautor($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = AutorPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setFichaAutor($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setInedito($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setSexo($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setNacionalidad($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setFechaNacimiento($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setNombre($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setApellido($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setVive($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setTipoautor($arr[$keys[9]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(AutorPeer::DATABASE_NAME);

		if ($this->isColumnModified(AutorPeer::ID)) $criteria->add(AutorPeer::ID, $this->id);
		if ($this->isColumnModified(AutorPeer::FICHA_AUTOR)) $criteria->add(AutorPeer::FICHA_AUTOR, $this->ficha_autor);
		if ($this->isColumnModified(AutorPeer::INEDITO)) $criteria->add(AutorPeer::INEDITO, $this->inedito);
		if ($this->isColumnModified(AutorPeer::SEXO)) $criteria->add(AutorPeer::SEXO, $this->sexo);
		if ($this->isColumnModified(AutorPeer::NACIONALIDAD)) $criteria->add(AutorPeer::NACIONALIDAD, $this->nacionalidad);
		if ($this->isColumnModified(AutorPeer::FECHA_NACIMIENTO)) $criteria->add(AutorPeer::FECHA_NACIMIENTO, $this->fecha_nacimiento);
		if ($this->isColumnModified(AutorPeer::NOMBRE)) $criteria->add(AutorPeer::NOMBRE, $this->nombre);
		if ($this->isColumnModified(AutorPeer::APELLIDO)) $criteria->add(AutorPeer::APELLIDO, $this->apellido);
		if ($this->isColumnModified(AutorPeer::VIVE)) $criteria->add(AutorPeer::VIVE, $this->vive);
		if ($this->isColumnModified(AutorPeer::TIPOAUTOR)) $criteria->add(AutorPeer::TIPOAUTOR, $this->tipoautor);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(AutorPeer::DATABASE_NAME);
		$criteria->add(AutorPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Returns true if the primary key for this object is null.
	 * @return     boolean
	 */
	public function isPrimaryKeyNull()
	{
		return null === $this->getId();
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Autor (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{
		$copyObj->setFichaAutor($this->ficha_autor);
		$copyObj->setInedito($this->inedito);
		$copyObj->setSexo($this->sexo);
		$copyObj->setNacionalidad($this->nacionalidad);
		$copyObj->setFechaNacimiento($this->fecha_nacimiento);
		$copyObj->setNombre($this->nombre);
		$copyObj->setApellido($this->apellido);
		$copyObj->setVive($this->vive);
		$copyObj->setTipoautor($this->tipoautor);

		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getAutorsRelatedById() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAutorRelatedById($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getLibros() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addLibro($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);
		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value
	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Autor Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     AutorPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new AutorPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Autor object.
	 *
	 * @param      Autor $v
	 * @return     Autor The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setAutorRelatedByTipoautor(Autor $v = null)
	{
		if ($v === null) {
			$this->setTipoautor(NULL);
		} else {
			$this->setTipoautor($v->getId());
		}

		$this->aAutorRelatedByTipoautor = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Autor object, it will not be re-added.
		if ($v !== null) {
			$v->addAutorRelatedById($this);
		}

		return $this;
	}


	/**
	 * Get the associated Autor object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Autor The associated Autor object.
	 * @throws     PropelException
	 */
	public function getAutorRelatedByTipoautor(PropelPDO $con = null)
	{
		if ($this->aAutorRelatedByTipoautor === null && ($this->tipoautor !== null)) {
			$this->aAutorRelatedByTipoautor = AutorQuery::create()->findPk($this->tipoautor, $con);
			/* The following can be used additionally to
				 guarantee the related object contains a reference
				 to this object.  This level of coupling may, however, be
				 undesirable since it could result in an only partially populated collection
				 in the referenced object.
				 $this->aAutorRelatedByTipoautor->addAutorsRelatedById($this);
			 */
		}
		return $this->aAutorRelatedByTipoautor;
	}

	/**
	 * Clears out the collAutorsRelatedById collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAutorsRelatedById()
	 */
	public function clearAutorsRelatedById()
	{
		$this->collAutorsRelatedById = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAutorsRelatedById collection.
	 *
	 * By default this just sets the collAutorsRelatedById collection to an empty array (like clearcollAutorsRelatedById());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAutorsRelatedById()
	{
		$this->collAutorsRelatedById = new PropelObjectCollection();
		$this->collAutorsRelatedById->setModel('Autor');
	}

	/**
	 * Gets an array of Autor objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Autor is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array Autor[] List of Autor objects
	 * @throws     PropelException
	 */
	public function getAutorsRelatedById($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collAutorsRelatedById || null !== $criteria) {
			if ($this->isNew() && null === $this->collAutorsRelatedById) {
				// return empty collection
				$this->initAutorsRelatedById();
			} else {
				$collAutorsRelatedById = AutorQuery::create(null, $criteria)
					->filterByAutorRelatedByTipoautor($this)
					->find($con);
				if (null !== $criteria) {
					return $collAutorsRelatedById;
				}
				$this->collAutorsRelatedById = $collAutorsRelatedById;
			}
		}
		return $this->collAutorsRelatedById;
	}

	/**
	 * Returns the number of related Autor objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Autor objects.
	 * @throws     PropelException
	 */
	public function countAutorsRelatedById(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collAutorsRelatedById || null !== $criteria) {
			if ($this->isNew() && null === $this->collAutorsRelatedById) {
				return 0;
			} else {
				$query = AutorQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByAutorRelatedByTipoautor($this)
					->count($con);
			}
		} else {
			return count($this->collAutorsRelatedById);
		}
	}

	/**
	 * Method called to associate a Autor object to this object
	 * through the Autor foreign key attribute.
	 *
	 * @param      Autor $l Autor
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAutorRelatedById(Autor $l)
	{
		if ($this->collAutorsRelatedById === null) {
			$this->initAutorsRelatedById();
		}
		if (!$this->collAutorsRelatedById->contains($l)) { // only add it if the **same** object is not already associated
			$this->collAutorsRelatedById[]= $l;
			$l->setAutorRelatedByTipoautor($this);
		}
	}

	/**
	 * Clears out the collLibros collection
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addLibros()
	 */
	public function clearLibros()
	{
		$this->collLibros = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collLibros collection.
	 *
	 * By default this just sets the collLibros collection to an empty array (like clearcollLibros());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initLibros()
	{
		$this->collLibros = new PropelObjectCollection();
		$this->collLibros->setModel('Libro');
	}

	/**
	 * Gets an array of Libro objects which contain a foreign key that references this object.
	 *
	 * If the $criteria is not null, it is used to always fetch the results from the database.
	 * Otherwise the results are fetched from the database the first time, then cached.
	 * Next time the same method is called without $criteria, the cached collection is returned.
	 * If this Autor is new, it will return
	 * an empty collection or the current collection; the criteria is ignored on a new object.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @return     PropelCollection|array Libro[] List of Libro objects
	 * @throws     PropelException
	 */
	public function getLibros($criteria = null, PropelPDO $con = null)
	{
		if(null === $this->collLibros || null !== $criteria) {
			if ($this->isNew() && null === $this->collLibros) {
				// return empty collection
				$this->initLibros();
			} else {
				$collLibros = LibroQuery::create(null, $criteria)
					->filterByAutor($this)
					->find($con);
				if (null !== $criteria) {
					return $collLibros;
				}
				$this->collLibros = $collLibros;
			}
		}
		return $this->collLibros;
	}

	/**
	 * Returns the number of related Libro objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Libro objects.
	 * @throws     PropelException
	 */
	public function countLibros(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if(null === $this->collLibros || null !== $criteria) {
			if ($this->isNew() && null === $this->collLibros) {
				return 0;
			} else {
				$query = LibroQuery::create(null, $criteria);
				if($distinct) {
					$query->distinct();
				}
				return $query
					->filterByAutor($this)
					->count($con);
			}
		} else {
			return count($this->collLibros);
		}
	}

	/**
	 * Method called to associate a Libro object to this object
	 * through the Libro foreign key attribute.
	 *
	 * @param      Libro $l Libro
	 * @return     void
	 * @throws     PropelException
	 */
	public function addLibro(Libro $l)
	{
		if ($this->collLibros === null) {
			$this->initLibros();
		}
		if (!$this->collLibros->contains($l)) { // only add it if the **same** object is not already associated
			$this->collLibros[]= $l;
			$l->setAutor($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Autor is new, it will return
	 * an empty collection; or if this Autor has previously
	 * been saved, it will retrieve related Libros from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Autor.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Libro[] List of Libro objects
	 */
	public function getLibrosJoinGenero($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = LibroQuery::create(null, $criteria);
		$query->joinWith('Genero', $join_behavior);

		return $this->getLibros($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Autor is new, it will return
	 * an empty collection; or if this Autor has previously
	 * been saved, it will retrieve related Libros from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Autor.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Libro[] List of Libro objects
	 */
	public function getLibrosJoinTematica($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = LibroQuery::create(null, $criteria);
		$query->joinWith('Tematica', $join_behavior);

		return $this->getLibros($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Autor is new, it will return
	 * an empty collection; or if this Autor has previously
	 * been saved, it will retrieve related Libros from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Autor.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Libro[] List of Libro objects
	 */
	public function getLibrosJoinMateria($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = LibroQuery::create(null, $criteria);
		$query->joinWith('Materia', $join_behavior);

		return $this->getLibros($query, $con);
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Autor is new, it will return
	 * an empty collection; or if this Autor has previously
	 * been saved, it will retrieve related Libros from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Autor.
	 *
	 * @param      Criteria $criteria optional Criteria object to narrow the query
	 * @param      PropelPDO $con optional connection object
	 * @param      string $join_behavior optional join type to use (defaults to Criteria::LEFT_JOIN)
	 * @return     PropelCollection|array Libro[] List of Libro objects
	 */
	public function getLibrosJoinTipopublicacion($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		$query = LibroQuery::create(null, $criteria);
		$query->joinWith('Tipopublicacion', $join_behavior);

		return $this->getLibros($query, $con);
	}

	/**
	 * Clears the current object and sets all attributes to their default values
	 */
	public function clear()
	{
		$this->id = null;
		$this->ficha_autor = null;
		$this->inedito = null;
		$this->sexo = null;
		$this->nacionalidad = null;
		$this->fecha_nacimiento = null;
		$this->nombre = null;
		$this->apellido = null;
		$this->vive = null;
		$this->tipoautor = null;
		$this->alreadyInSave = false;
		$this->alreadyInValidation = false;
		$this->clearAllReferences();
		$this->resetModified();
		$this->setNew(true);
		$this->setDeleted(false);
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collAutorsRelatedById) {
				foreach ((array) $this->collAutorsRelatedById as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collLibros) {
				foreach ((array) $this->collLibros as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collAutorsRelatedById = null;
		$this->collLibros = null;
		$this->aAutorRelatedByTipoautor = null;
	}

	/**
	 * Catches calls to virtual methods
	 */
	public function __call($name, $params)
	{
		// symfony_behaviors behavior
		if ($callable = sfMixer::getCallable('BaseAutor:' . $name))
		{
		  array_unshift($params, $this);
		  return call_user_func_array($callable, $params);
		}

		if (preg_match('/get(\w+)/', $name, $matches)) {
			$virtualColumn = $matches[1];
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
			// no lcfirst in php<5.3...
			$virtualColumn[0] = strtolower($virtualColumn[0]);
			if ($this->hasVirtualColumn($virtualColumn)) {
				return $this->getVirtualColumn($virtualColumn);
			}
		}
		return parent::__call($name, $params);
	}

} // BaseAutor
